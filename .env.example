# Get a token at https://ipinfo.io — used to lift the free-tier rate limit.
# Copy this file to `.env` (gitignored) and fill in the value.
# Also picked up from the IPINFO_TOKEN environment variable (e.g. direnv + pass).
IPINFO_TOKEN=
